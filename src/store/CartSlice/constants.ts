export enum CART_ACTIONS {
    SET_CART = "setCart",
    SET_CART_SUCCESS = "setCartSuccess",
    SET_CART_FAILURE = "setCartFailure"
}

export enum BUTTON_TEXT {
    ADD_TO_CART = "Добавить в корзину",
    DELETE_FROM_CART = "Удалить из корзины"
}

export enum LOAD_STATUSES  {
    UNKNOWN = 'unknown',
    LOADING = 'loading',
    LOADED = 'loaded',
    ERROR = 'error',
}